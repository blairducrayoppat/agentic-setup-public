#pragma once
// Core logic for the app. Replace/extend `add` with the task's real functionality,
// keeping declarations here and definitions in core.cpp, with a matching test.
namespace app {
int add(int a, int b);
}
