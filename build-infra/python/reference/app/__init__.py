"""app -- the project's package. Put the task's modules here and import them as
`from app.<module> import ...`. Extend this skeleton; do not start a second top-level
module. House rules for new code and tests -- the injected-I/O seam for interactive
entry points, and tmp_path-isolated test data -- are in AGENTS.md."""

__version__ = "0.1.0"
