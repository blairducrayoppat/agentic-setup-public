# Critic verdict

## CONFLICTS
1. OVMS LOGGING (3-way: scripts vs ovms-ops vs novice-ops). Methods conflict (Start-Process -RedirectStandardOutput/-RedirectStandardError vs native --log_path), directories conflict (state\logs vs agentic-setup\logs), retention conflicts (keep-newest-20 vs 14-day prune), and the two AI Status drafts tail different globs (ovms-*.err.log vs ovms-*.log). WINNER: stream redirection to dated files under state\logs, keep-newest-20. Redirection is a strict superset of --log_path: it also captures pre-logger output (bad-flag parse errors, port-bind failures, crash text) — exactly the forensics this fix exists for. ovms-ops' --log_level INFO point is moot (INFO is the default and logger output goes to the redirected streams anyway). AI Status standardizes on tailing the newest *.err.log; novice-ops' one-time move of the stray root ovms-30b.log/ovms-test.log into state\logs is merged in.
2. STOP-LLM.PS1 (2 incompatible full rewrites). scripts auditor: stop ovms + offer restart of VMs recorded in state\stopped-vms.txt. novice-ops: stop ovms + clear fleet-mode.flag. WINNER: the VM-restart rewrite — it fixes a live, reproduced loss (flag exists, BlarAI-Orchestrator is Off right now, nothing ever offers the restart). The fleet version dies with the rejected watchdog. ovms-ops' two-line "hard-stop is safe / delete ov_cache if a first-load was interrupted" message is merged into it.
3. MODEL PINNING (scripts' launch-time `opencode -m local/<loaded-id>` vs opencode-harness' persistent OPENCODE_CONFIG overlays via [Environment]::SetEnvironmentVariable(...,'User')). WINNER: the -m pin. It is stateless, derived live from /v3/models at launch, and fixes the exact mismatch the user already hit. The persistent User-scope env var is invisible global state that goes stale after a crashed starter (the proposer admits this) — a novice debugging nightmare. small_model title-gen 404s are unproven; if ever observed, use a PROCESS-scoped $env:OPENCODE_CONFIG set by open-coding.ps1, never a persistent one.
4. READINESS CHECK (scripts' id-match poll on /v3/models vs ovms-ops' /v2/health/ready). WINNER: id-match. /v2/health/ready returns 200 without naming the model, so it cannot catch the wrong-model-READY bug (already hit) nor a foreign listener answering on 8000; the id check catches both. /v2/health/ready is recorded in BLUEPRINT as the contract for any future watchdog.
5. SAMPLING TEMPERATURE (opencode-harness: "opencode injects 0.55 for Qwen, set agent temp 0.7/0.8" vs ovms-ops: "generation_config.json already ships the recommended 0.7/0.8/top_k 20 — nothing to change"). Both are right at different layers: server defaults are correct, but any temperature opencode SENDS overrides them. RESOLUTION: no OVMS-side change; keep the agent-level 0.7/0.8 pin as cheap nice-to-have insurance — worst case it re-asserts the values the server already uses.
6. 05-OVERNIGHT-POWER SEVERITY (scripts: "highest-consequence silent failure" vs novice-ops live verification: standby-timeout-AC already 0, so the script evidently ran successfully elevated). WINNER: novice-ops' evidence. Severity downgraded from urgent to routine; the 5-line admin guard survives only as cheap protection for future re-runs.
7. VISION RAM (scripts: raise needGB 8→10 and optionally per-branch cache_size vs ovms-ops: multi-model graph.pbtxt with cache_size 1-2). Multi-model is rejected outright, so the simple needGB raise wins, folded into the per-model reclaim-table edit (same file, same numbers block) and re-verified against the first captured vision log.
8. WATCHDOG vs MANUAL-STOP SEMANTICS: the fleet flag protocol existed to stop the watchdog fighting "Stop AI Models". Moot after rejection, but the rule is recorded for Phase 8: any future watchdog must be sentinel-gated and stop-llm must clear the sentinel.
9. LOCKED-CONSTRAINT CHECKS: the stock OVMS Windows service (binds 0.0.0.0, runs LocalSystem) violates loopback-only — ovms-ops correctly proposed NOT installing it; kept as a BLUEPRINT guard note with the corrected sc-config line. The Ollama-hardcoded vision plugin (would force a second model stack into RAM) was rejected by its own auditor — rejection confirmed.

## REJECTED
1. FLEET-MODE WATCHDOG + 'Fleet Mode ON/OFF' launchers + hidden scheduled task (novice-ops P0). Speculative: overnight fleet runs are Phase 8 and OpenClaw is not installed. It adds a background task a novice cannot see or debug, a new flag protocol, two more Desktop launchers, and unattended auto-restart of an 18GB model load on a 31.3GB machine with no human watching the memory assistant — RAM-risk plus complexity for a problem not yet hit. Revisit at Phase 8 together with the Read-Host guard.
2. PERSISTENT OPENCODE_CONFIG OVERLAYS via User-scope environment variable (opencode-harness P0/medium). Persistent invisible machine state mutated by starter scripts; proposer admits a crashed starter leaves a stale mispinned overlay. The -m launch pin solves the real (already-hit) mismatch with zero stored state. The small_model 404 it targets is theoretical — defer until a failed title is actually observed, then use a process-scoped env var only.
3. MULTI-MODEL 'everyday+vision' config.json mode (ovms-ops P2/medium). Speculative capability; requires hand-authoring two graph.pbtxt files whose syntax errors fail the whole servable load; 14B+VL+KV caches ≈ 19-20GB resident on a machine idling at ~15GB used leaves dangerously little headroom; contradicts the locked one-model-resident design. The swap workflow already covers vision.
4. GENERIC MULTI-VM ENUMERATION in start-llm (scripts P1/medium). The HAOS VM it future-proofs for does not exist; only BlarAI-Orchestrator exists and the hardcoded 2.0GB is accurate today. Medium refactor for a hypothetical = rejected. SALVAGED: only the flag relocation from purge-prone %TEMP% to state\stopped-vms.txt (real defect).
5. READ-HOST NON-INTERACTIVE GUARD (scripts P1). No non-interactive caller exists today — every entry point is a double-click and -Force already exists for automation. Bundle with Phase-8 work when a real scheduled caller appears.
6. LSP DISABLE (opencode-harness P2). The proposer's own verdict is "keep on by default"; post-edit diagnostics are how a small local model catches its own broken edits. Document the lever in BLUEPRINT, change nothing.
7. BITDEFENDER EXCLUSION of C:\models now. Security regression on a security-first machine; only acceptable after measuring that scanning is actually the load-time bottleneck. Stays a documented conditional, not a plan item.
8. VISION PLUGINS (opencode-image-comprehension et al.). Ollama-hardcoded (cannot target OVMS), third-party code, and would double model RAM. Rejection confirmed; native vl-swap workflow stands.
9. SMALL_MODEL pinning machinery in any form, for now. Unproven failure; revisit only on observed title-gen 404s.

## FINAL PLAN
1. [P0-prep] git init C:\Users\mrbla\agentic-setup as LOCAL-ONLY repo (.gitignore: state/, *.log), baseline commit + one config zip BEFORE any edits below — the restore point for this whole wave.
2. [P0] start-llm.ps1: replace line 170 Start-Process with dated -RedirectStandardOutput/-RedirectStandardError files under state\logs (keep newest 20); failure paths (lines 192/194) tail last 15 lines of the .err.log and print its path; one-time move of stray root ovms-*.log into state\logs.
3. [P0] start-llm.ps1: replace flat $reclaimFromOvms=9 (line 69) with per-model table from /v3/models (coder-30b 18 / qwen3-14b 10 / qwen3-vl-8b 6, fallback 6); in the same numbers pass raise vision needGB 8→10 (6GB weights + 4GB cache); re-verify both against the first captured logs.
4. [P0] start-llm.ps1: port-8000 pre-flight (throw with owner name if a non-ovms process listens) inserted before line 159, and readiness poll (lines 176-178) must id-match $name with -UseBasicParsing before printing READY — kills wrong-model-READY and false READY from foreign listeners.
5. [P0] start-llm.ps1: record stopped VM names in state\stopped-vms.txt instead of the %TEMP% flag (Storage Sense purges %TEMP%); keep the existing hardcoded BlarAI-Orchestrator logic otherwise; remove a name from the file only when the VM is actually restarted (y), not on decline.
6. [P0] stop-llm.ps1: rewrite — stop ovms, then offer restart for each VM in state\stopped-vms.txt (honor legacy %TEMP% flag one transition period; the flag is set and the VM is Off RIGHT NOW); append the 'hard-stop is safe; if a load was interrupted delete C:\models\ov_cache' note.
7. [P0] open-coding.ps1: keep the /v3/models result (line 14) and launch `opencode -m local/<loaded-id>` when the id is one of the three known models; bare opencode fallback with a yellow notice otherwise — ends the default=qwen3-14b mismatch already hit.
8. [VISIBILITY] New ai-status.ps1 + 'AI Status.cmd' (merged version): server state + loaded model id, memory with 23/26GB color thresholds, ovms working set, VM states + stopped-VM note, port-8000 foreign-listener warning, AC/battery, disk-free, Arc driver vs known-good 32.0.101.8826, tail of newest state\logs\*.err.log. Read-only. No fleet-mode line.
9. [VISIBILITY] Open Coding Chat.cmd: append `if errorlevel 1 (echo Something went wrong above... & pause)` so unhandled errors stay readable.
10. [VISIBILITY] Replace all Desktop .cmd copies (incl. new AI Status.cmd) with 2-line `call` stubs pointing at the agentic-setup originals — drift becomes structurally impossible (Stop AI Models.cmd timestamps already diverge).
11. [HARDENING] opencode.json: permission block — bash glob asks for rm/del/Remove-Item/git push/git reset --hard/git clean/git checkout --, format deny, doom_loop ask, edit allow; ADDITIONALLY read-protect $env:USERPROFILE\.ssh\* and .git-credentials (see missed item 1).
12. [HARDENING] Create global C:\Users\mrbla\.config\opencode\AGENTS.md (~12 lines small-model discipline: smallest diff, no invented paths, stop after two identical failures, ask before destructive ops).
13. [HARDENING] opencode.json context-burn: tools.websearch/webfetch=false (offline-first), compaction {auto true, prune true, tail_turns 2}, tool_output {max_lines 800, max_bytes 32768} — directly targets the compaction-loop class already hit.
14. [HARDENING] open-coding.ps1: (a) OneDrive warn-and-confirm in the Browse path (Documents IS OneDrive-redirected); (b) home-repo guard — if the picked folder has no .git of its own, offer `git init` since C:\Users\mrbla itself is a git repo (see missed item 1); (c) change line 64 to `git -C $target -c init.defaultBranch=main init *> $null` (PS5.1 NativeCommandError fix).
15. [HARDENING] new-agent-task.ps1: sanitize -Task to [\w\-], check server AND loaded-model-matches-$Model BEFORE `git worktree add`, check $LASTEXITCODE after worktree add and after opencode run.
16. [HARDENING] .wslconfig: add memory=4GB / processors=4 under the existing mirrored line (timestamped backup per existing convention) — zero impact until WSL exists, permanently disarms the 50%-RAM balloon.
17. [HARDENING] One elevated command: outbound firewall block for C:\ovms\ovms.exe (verified costless — downloads go via hf-cli; loopback serving unaffected); record rule name + disable command in BLUEPRINT.
18. [HARDENING] 04-seed-offline.ps1: gate npm/pnpm registry switch on a successful Verdaccio /-/ping, guard the null verdaccio.cmd path, add --with pip to both uv wheelhouse lines. 05-overnight-power.ps1: 5-line hard admin check (settings verified already applied; guard protects re-runs).
19. [NICE] backup-config.ps1 / restore-config.ps1 + two launchers (12 rotating zips of live opencode.json/.wslconfig/scripts/configs/launchers, auto local git commit); decide explicitly whether to include opencode session history (missed item 5); must land before OpenClaw Phase 4 per BLUEPRINT section 10.
20. [NICE] check-updates.ps1 + 'Check AI Updates.cmd' (check-only: opencode/OVMS/OpenClaw/driver drift; prints the never-`npm update -g` rule); optional 4-weekly scheduled nudge.
21. [NICE] Shift Windows Update active hours 11:00→05:00 to 13:00→07:00 (Settings-UI path for the novice; closes the 05:00-07:00 auto-restart window).
22. [NICE] opencode.json agent.build/agent.plan temperature 0.7 / top_p 0.8 (insurance only; server generation_config already correct).
23. [NICE] Recycle ~\.ollama (16.8GB orphaned blobs, Recycle-Bin method, recoverable; space returns when bin is emptied after a week of normal use).
24. [NICE] Single BLUEPRINT.md update: service-installer-binds-0.0.0.0 warning + corrected sc config line; Qwen3.5/3.6 prefix-caching RAM bug + issue #4230 (30-90s compile every start is expected); hard-kill/ov_cache note; keybind cheat-sheet (f2, ctrl+x m, tab, ctrl+x c); vision-via-swap no-plugins policy; keep the known-good Arc driver installer on disk; Bitdefender exclusions only-if-measured; /v2/health/ready as the future watchdog contract.
25. [VERIFY] 5-minute smoke checklist appended to BLUEPRINT and executed once: start 14B → READY names qwen3-14b → AI Status shows it → Stop AI Models → VM restart offer appears → log file exists in state\logs (missed item 6).

## MISSED
1. HOME DIRECTORY IS A GIT REPO — the biggest miss. C:\Users\mrbla itself has a .git, and open-coding.ps1's [B]rowse path runs git init only for NEW projects: a browsed folder without its own .git lets every agent git command walk up and operate on the ENTIRE home directory repo — an agent-issued `git clean -fd` or `git reset --hard` there is catastrophic, and it sits right next to plaintext .git-credentials and .ssh keys that no auditor shielded from agent reads. Fix is folded into plan items 11 and 14 (read-protection globs + offer-git-init guard).
2. SEQUENCING: all four ordered their proposals without noticing the baseline backup/git-init must come FIRST — this wave rewrites stop-llm entirely and patches five other scripts; doing it without a restore point contradicts the very data-loss discipline three of them preached. Now plan step 1.
3. FLAG-ON-DECLINE BUG carried into the fixes: both current start-llm (line 188) and the proposed stop-llm rewrite delete the stopped-VM flag even when the user answers 'N' — one decline and the restart offer is lost forever, recreating the exact silent-dead-BlarAI failure being fixed. Remove a name only on 'y' (folded into plan items 5-6).
4. SWAP LEAVES ZERO MODELS ON FAILURE: start-llm kills the old ovms (line 159) before the new model proves loadable, so a failed swap strands the user with nothing running and no statement of that fact. Cheap fix: the failure message should say 'your previous model was already stopped — double-click a launcher to reload' (fold into plan item 2's new error text).
5. OPENCODE SESSION HISTORY (~\.local\share\opencode) holds the user's chat/task history — user data the backup proposal silently omits; include it or document the exclusion deliberately.
6. NO VALIDATION STEP: none of the four proposed verifying their own patches; for a novice operator a one-time scripted/checklist smoke test (start → READY id → status → stop → VM offer → log exists) is the difference between 'changed' and 'working'. Now plan step 25.
